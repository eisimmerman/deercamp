(function(){window.DeerCampRoomHover={init(root=document){const tip=document.createElement('div');tip.className='room-tooltip';document.body.appendChild(tip);root.querySelectorAll('[data-tooltip]').forEach(el=>{const show=()=>{tip.textContent=el.dataset.tooltip;tip.classList.add('show')};const hide=()=>tip.classList.remove('show');el.addEventListener('mouseenter',show);el.addEventListener('focus',show);el.addEventListener('mouseleave',hide);el.addEventListener('blur',hide);el.addEventListener('mousemove',e=>{tip.style.left=Math.min(innerWidth-290,e.clientX+14)+'px';tip.style.top=Math.min(innerHeight-55,e.clientY+14)+'px'})})}}})();
